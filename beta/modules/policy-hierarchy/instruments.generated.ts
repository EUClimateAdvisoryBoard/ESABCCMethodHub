/**
 * EU Policy Hierarchy — instrument register.
 *
 * GENERATED: compiled by one Sonnet agent per domain branch (orchestrated
 * via a Fable workflow, July 2026), then assembled into this literal.
 * Entries with `confident: false` link to a EUR-Lex search rather than a
 * CELEX record and need human verification before citation.
 */
import type { DomainBranch } from './data';

export const DOMAIN_BRANCHES: DomainBranch[] = [];
