/**
 * EU Policy Hierarchy — per-lens relevance rationales.
 *
 * GENERATED: compiled by Sonnet agents (one per branch group, July 2026).
 * For every lens tag on every instrument, one concrete clause completing
 * "<Lens>-relevant because …" — shown in the dot tooltips and, when a lens
 * is active, inline on matching rows. AI-drafted, pending review.
 */
import type { LensId } from './data';

export const LENS_RATIONALES: Record<string, Partial<Record<LensId, string>>> = {};
