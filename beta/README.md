# `beta/` — experimental modules (not shipped in v1.0)

This folder is a **deliberate parking lot**. It contains the Next.js page
folders for the modules that are *not* part of the v1.0 handoff to EEA IT.

The production deployment ships the **eight stable modules** under
[`src/app/`](../src/app). Nothing in this folder is routed by the live app:
the files live outside the Next.js `app/` tree, so the build simply ignores
them. Dropping a folder back into `src/app/` re-activates its route — that is
the whole migration path for promoting a beta module.

## Why this separation exists

When EEA IT, or any reviewer from outside CCE5, opens the repository they
should see the **working scope** without having to guess which surfaces are
production and which are still prototypes. The file system itself communicates
that:

```
src/app/           ← production routes (the eight modules + utilities)
beta/modules/      ← experimental prototypes, unrouted
```

The eight production modules have been hardened: schema migrations, RLS
policies, GDPR retention, data-pipeline tests, IT handoff scripts. The beta
modules are useful demos of *where this platform can go* — energy-system
optimisation, climate-risk chains, media monitoring — but they are not yet at
the quality bar we ship to the Secretariat. (The Project Workspace and
Recommendations modules made exactly this journey: they were promoted out of
`beta/` into the core once hardened.)

## What is in here

| Folder                        | Module               | Why it is beta                                                     |
|-------------------------------|----------------------|--------------------------------------------------------------------|
| `climate-adaptation/`         | Climate Adaptation   | CLIMADA + CMIP6 hotspots; needs full impact-chain validation.      |
| `climate-finance/`            | Climate Finance      | NGFS v5 scenarios; EIB green-bond dataset not yet licensed.        |
| `energy-system/`              | Energy System        | PyPSA-style optimisation; requires PyPSA backend service.          |
| `fact-sheets/`                | Fact Sheet Builder   | Drag-and-drop widgets; LaTeX export path not yet validated.        |
| `faq/`                        | FAQ & Prebunking     | Content pipeline; awaiting editorial sign-off.                     |
| `funding-sources/`            | Funding Sources      | Horizon Dashboard + DG DIGIT QlikSense snapshot; scraper pending.  |
| `maritime-aviation/`          | Maritime & Aviation  | SEAMAPS / OAG data requires procurement.                           |
| `media-monitoring/`           | Media Monitoring     | GDELT pipeline works; dashboard UX and consent flow WIP.           |
| `strategy-docs/`              | Strategy & Framework | Documentation overlay for internal strategy and PIRs; files pending upload. |
| `eu-climate-councils/`        | EU Climate Councils  | Leaflet map of ~67 national advisory bodies; mapping being verified. |
| `project-management/`         | Project Management   | Phase / Gantt board against the Project Manual; UX iterating.      |
| `national-climate-policies/`  | National Level Climate Policies | EU-27 laws & policies from climate-laws.org (CC-BY 4.0); in-module refresh via Climate Policy Radar API + Supabase snapshot. Includes an **EU-policy → national implementation tracker** subpage (`/implementation`): for each EU policy requiring national implementation (Effort Sharing, RED, EPBD, AFIR, LULUCF, CAP…) it rates each member state, flags who is lagging, and pulls out best-practice national instruments for the laggards — plus an at-a-glance policy-architecture monitor. See `analysis/POLICY-MONITORING.md`. |
| `transition-panorama/`        | EU Transition Panorama | Panorama-Sweden-style radial explorer of EU emissions vs the AR6-based 2040 advice scenarios; will be re-based on the AR7 scenario database once published. |
| `transition-stories/`         | Transition Stories | Cinematic scroll-driven data essay on real ESABCC report photography: scrollytelling pathway chart, parallax chapters, sector ledger. Imagery sourced from the published report covers. |
| `transition-stories-2/`       | Transition Stories 2 — Mind the Gap | Cinematic, oryzo.ai-inspired scroll experience for the policy gap report (*Towards EU climate neutrality*, 2024): inertia-smoothed scroll-shaded gap chart, clip-path photo reveals, living SVG scenes (water, embers, cattle, cyclist) over real stock photography (Pexels), sector gap ledger, the 13 key recommendations. |
| `policy-coherence/`           | Policy Coherence Assessment | Rule-based four-step coherence model: Assumption-Based Planning audits (RAND), Nilsson et al. (2016) seven-point goal-interaction scores, goals/means congruence derived from the objective–delivery checklist, and EEA-style distance-to-target pace ratios. Observations are AI-collected baselines (snapshot mid-2026) pending source re-verification. |
| `ai-environmental-impact/`    | AI Environmental Impact | Interactive estimator of the GHG footprint of *using* AI (inference): EU vs US grid carbon intensity, energy per token, and token budgets for four reference tasks (a normal question, a short writing task, a deep-research run, writing ~1,000 lines of code), each with a worked example. Compares the result to everyday anchors (web search, kettle, driving 5 km, a beef burger, an hour of flying) and scales one prompt to a planetary query volume to surface the grid-expansion challenge. Adds a macro panel tracking the hyperscalers' rising — and accelerating — electricity-per-dollar-of-revenue from their own CSR/sustainability reports (Microsoft, Google, Meta, 2020–2024), and a self-measurement of the CO₂e cost of building MethodHub itself. Order-of-magnitude reconstruction from public figures (EEA, EPA eGRID, IEA *Energy and AI*, Epoch AI, Google, company sustainability reports), not a measurement; all assumptions are sliders. |
| `ets-wishlist-impact/`        | ETS Wishlist — GHG Impact | Transparent, slider-driven accounting model of the GHG cost of the EPP's ten-point "wishlist" to soften the EU ETS (slower LRF, weaker MSR, removal & international credits, slower CBAM free-allowance phase-out, free allocation beyond 2030, looser benchmarks, conditional free permits, ETS-revenue earmarking, intra-EU-only aviation). Each demand is modelled one-by-one and combined, organised into Conservative/Central/Maximalist scenarios, with the headline net setback expressed as years of EU progress lost and as a share of the ESABCC-advised 2030–2050 carbon budget. Honest core: cap/supply demands (LRF, MSR, offsets) drive real extra cumulative emissions; free-allocation demands are near-neutral on the capped total (modelled as indirect leakage). Every instrument links to the EUR-Lex legislation. Order-of-magnitude reconstruction, not a market simulation. |
| `policy-analysis-cookbook/`   | Policy Analysis Cookbook | One-to-one visualisation of the internal position paper *Suggestion for a policy analysis cookbook*: the three-phase method (prepare → coherence analysis → integration), the sector/system scoping options, the first/second/third-order policy onion, Box 1 theory, Figure 1 (analysis steps matched to the first Policy Gap report gap categories) and Figure 2 (the policy cycle, after Henstra 2015 / Better Regulation toolbox), plus the Step-2 coherence codebook and key questions. Static method walkthrough; content taken verbatim from the paper. |
| `short-formats/`              | Short Formats | Four compact, one-page briefs on **ETS**, **LULUCF**, the **energy crisis** and **flexibilities for international carbon mitigation**, with two views. *Recommendations*: the Board's advice itself, drawn **only** from existing ESABCC recommendations (looked up by id from `src/data/esabcc-recommendations.ts`), each card linking back to its source report — nothing invented. *Political reality*: a Secretariat strategic overlay (grounded in the recorded legislative-tracking events) classifying each recommendation as a **live window** (speak now), an **open gap** (put it on the agenda) or **moved on** (effectively decided/overtaken — reframe rather than restate), so the board can see where its advice can still land and where the discussion has run too far ahead (e.g. the 5% international credits, decided in Reg (EU) 2026/667). The overlay is clearly flagged as editorial, not report content. |
| `digital-energy-roadmap/`     | Digital & AI Energy Roadmap — Coherence Check | Structured side-by-side reading of the Commission's *Strategic Roadmap for Digitalisation and AI in the Energy Sector* (COM(2026) 501 final, 3 Jun 2026) against the ESABCC recommendations and the EU's binding climate goals. Every Roadmap commitment is classified as a **contradiction**, **tension**, **ambition gap** or **alignment**, with a near-verbatim Roadmap quote + source page, the specific ESABCC recommendation id (resolved live from `esabcc-recommendations.ts`), the EU goal at stake, and a numbered reasoning chain. Headline tension: the plan to *triple* EU data-centre capacity (12 GW → 28 GW by 2030) runs against KR12's demand-reduction call and the EED's absolute energy cut, leaning on voluntary instruments. Alignments (smart grids, ≈230 GW flexibility, curtailment reduction) included for balance. Data in `src/data/digital-energy-roadmap.ts`. |
| `ex-post-analysis/`           | Ex-Post Policy Assessment | Faithful rendering of the internal *Ex-Post Policy Assessment — Methods Scoping Note* (working brief for Policy Gap Report 2.0). Argues that ex-post method selection is driven by **counterfactual availability**, not by the mitigation/adaptation split. Builds the two-axis design grid (domain × counterfactual), the per-instrument quant identification table with cleanliness ratings (ETS DiD/RD, CO₂-standard bunching, RED scheme-design switches, EED/EPBD efficiency gap, ESR/LULUCF → contribution analysis, CBAM/ETS2/SCF too new), why quant ex-post breaks for adaptation (non-stationary baseline, rare-event tests, no tCO₂e) and the four-layer qual construction (contribution analysis spine, realist CMO, process tracing, hazard-variation case selection), the adaptation quant islands (Floods Directive, Solidarity Fund), the five design rules and the per-instrument method justifications. Static method walkthrough; all ratings and justifications taken verbatim from the note. |
| `sector-background/`          | Sector Background | Structured background brief for the two sectors the lead owns (**Industry** & **Transport**). Reuses the Project-Workspace assessment-framework **flow chart** (`SectorFlow`, from `src/data/sector-frameworks.ts`) — published report view by default, with a toggle to the Enhanced + adaptation board to show how the methodology can be improved. Around the flow chart it lays out mitigation options (from the levers), adaptation options (from the beta EUCRA layer), the EU **policies to look into** (live from `src/data/sectoral-policies.ts`, each linked to EUR-Lex) and a curated **reading list of influential papers** (AI-curated, pending source re-verification). Closes with a "from a static figure to an analysis surface" methodology-improvement section pointing at the alternative prototyped boards. |
| `overview-industry/`          | Overview Industry (Summer Prep sub-module) | Cross-cutting overview surface for EU industrial decarbonisation with two sub-pages. **Clean Tech** (`/cleantech`) is a big, collapsible, fully-sourced catalogue mapping the EU-27 industry emission profile → each energy-intensive subsector → every available mitigation technology → its marginal abatement cost, technology readiness (TRL), commercial availability, real project pipeline (incl. Final Investment Decisions) and a plain-language rationale for why costs are high, what holds readiness back and what makes scaling hard — every data point carries a real source link (EEA/EU ETS, IPCC AR6 Ch.11, IEA roadmaps/databases, Material Economics, LeadIT, IEAGHG, Global CCS Institute, peer-reviewed journals, project disclosures), so it can be mined for recurring bottlenecks that inform policy. A **clean-tech vs old-tech** overlay (`TECH_CLASSIFICATION` / `TECH_CLASS_META` in `cleantech-catalogue.ts`) re-colours the lever ring to separate the near-zero endpoints (electrification, green hydrogen, ore electrolysis, recycling, unavoidable-process-CO₂ capture) from the incumbent-based transitional bridges (natural-gas/&ldquo;blue&rdquo; hydrogen, blast-furnace CCS, fossil fuel-switching) — a clearly-flagged analytical judgement, not a sourced datum. Its literature list is seeded into the Reference Manager + Industry Project Workspace under a new **Clean Tech** chapter tag (`src/data/clean-tech-reading-list.ts`, seeded via `ensureCleanTechReadingSeed` in `project-workspace/db.ts`). **Trade flows** (`/trade-flows`) is an input–output map of EU-27 manufacturing trade across all 24 NACE Section C divisions, organised as one summary **Overview** dashboard (headline facts, diverging import-vs-export trade balance, import-dependency map of import reliance × supplier concentration, critical-materials board), a **Dependencies dashboard** — every curated critical trade dependency (CRMA raw materials with their legal Annex I/II designations, the SWD(2021) 352 strategic product families, live Eurostat energy dependency) in one sortable register, aggregated by supplier country and by NACE division — plus a **division deep-dive**: pick one Section C division and see all its information in one place — trade balance and intra/extra-EU split, the imported-input mix from the EU-27 use table, FIGARO import origins / export destinations / foreign value added, curated critical inputs and the division's own import dependencies. The **full FIGARO inter-country input–output table** (`naio_10_fcp_ii4`, ~11 M cells, 2022–2023) is imported into the repo by `scripts/fetch-figaro-io-dataset.mjs`, condensed to the EU-27-as-one-economy account in `public/data/figaro/`, and explorable at `/trade-flows/figaro` as a filterable table viewer + analysis dashboard with CSV export — the IO data lives on the MethodHub, not behind an external link. The trade backbone is live Eurostat data (`ext_tec01`, EU-27, 2023+2024, pulled from the dissemination API); the dependency layer sits on the EC/JRC strategic-dependency reviews (SWD(2021) 352), the Critical Raw Materials Act (Reg. (EU) 2024/1252), OECD TiVA foreign value added and Eurostat energy statistics — every figure carries a source link. Data in `trade-flows/trade-data.ts`. |
| `electricity-prices/`         | Electricity Prices — Why Europe Pays More | Scientific EU vs US vs China explainer of the electricity price gap. Historic panel 2015–2025 (German day-ahead vs EIA hub average vs China's administered coal-anchored band, plus TTF vs Henry Hub vs Qinhuangdao coal in €/MWh thermal), an **interactive merit-order model** (uniform-price auction; MC = fuel/η + (EF/η)·CO₂ + O&M; presets EU 2019/Aug-2022/2025, US 2025, hypothetical China spot) showing the gap is an *input* gap — imported LNG at 3–5× Henry Hub plus the EU ETS — not a mechanism gap, a same-machine-three-fuel-bills decomposition (of the ≈€71/MWh EU–US marginal-generator gap, ≈€48 is fuel, ≈€25 carbon), the retail stack (≈half an EU household bill is network + taxes/levies; China holds households *below* cost via industry cross-subsidy — the inverse of the EU) and a six-finding diagnosis incl. the merit-order effect (renewables lower, not raise, the wholesale price) and the carbon-intensity flip side of Chinese cheapness. Order-of-magnitude reconstruction (SMARD, EIA, Eurostat, JRC/ACER, RMI/NDRC, IEA, Draghi report); all inputs are sliders. |
| `ets-cdr-price/`              | ETS Endgame & CDR Safety Valve | Interactive reduced-form reconstruction of the Ariadne dossier *A Safety Valve for the EU ETS Endgame* and the PIK/LIMES-EU Joule paper *Sequencing CDR into the EU ETS* (Sultani, Osorio, Pahle, Edenhofer et al., 2026). Banking/Hotelling allowance-price model with a convex MACC and a price-responsive permanent-CDR backstop; sliders for the cap trajectory, abatement economics, CDR techno-economics and the sequencing policy (admission year, volume, integrity haircut, MSR docking). Stylised teaching/what-if tool, not the original LIMES-EU model; defaults calibrated to the published anchors (≈€203/2030, ≈€353/2040, 68–86 Mt CDR/yr by 2050). Includes a `/status` subpage — a curated snapshot of the real EU CDR pipeline (LULUCF land sink, operational/FID/announced BECCS, DACCS, biochar & enhanced-weathering projects) and the gap to the volumes the valve needs. |
| `summer-prep/`               | Summer Prep | **M · 35** (formerly M · 37). A working space bundling the industry & transport preparation for the next progress report. **Overview Industry** and the **Policy Gap Tracker** — formerly the stand-alone modules M · 34 and M · 36, now retired from the landing-page list — live here as sub-modules (their routes stay alive), plus three new internal notes: **Indicator Check** (`/indicator-check`), a dashboard over the old report's progress indicators showing the latest two–three data points added since publication and the arithmetic change against the report baseline — no better/worse read is applied (read live from the Policy Gap 2.0 Project Workspace indicator database — the same `listIndicators` read the workspace renders from — so it only ever shows data that is also in the workspace); **Synergies & trade-offs** (`/synergies-tradeoffs`), a literature note mapping mitigation ↔ adaptation synergies and trade-offs for industry & transport, kept in the report's own subsector structure (data + IPCC/EUCRA/peer-reviewed citations in `summer-prep-synergies.ts`); and **Policy gaps — Transport & Industry** (`/policy-gaps-sectors`), which reassesses whether each sector gap still exists after legislation adopted since the report, proposes candidate additional gaps, and draws the subsector × gap-type landscape (overlay in `summer-prep-sector-gaps.ts`). The reassessment and candidate gaps are AI-compiled working drafts flagged pending verification. |
| `ets-review/`                 | ETS Review & Electrification | **M · 37**. Hub for the 17 July 2026 Commission package — the **Electrification Action Plan** (COM(2026) 595) and the **ETS Phase-5 review** (COM(2026) 616, 2031–2040) — split into two matching submodules. **Electrification** (`/electrification`) is an interactive least-cost model in exact numerical parity with `ets-review/model/electrification_lcm.py`: the carbon price needed to hit the indicative 46%-by-2040 electrification target (vs ~23% today) on a price-only path (≈€166/t) versus with the Action Plan's demand-side package (≈€55/t), the ≈€111/t gap read as the shadow value of demand-side policy, driven by sliders over the target, baseline, barrier costs and per-sector measure strength. **ETS reform** (`/reform`) is a sourced overview of the proposed changes — cap trajectory (LRF 3.7% → 1.7%), MSR reform (24% → 12%), 250 Mt permanent removals, ≈260 Mt international credits, the €100bn Industrial Decarbonisation Bank, free allocation and CBAM phase-out to 2038 — each card linked into the Commission's own communication (press release, Q&A, COM(2026) 616), plus a filterable uncertainty & ambiguity register drawn from the impact assessment SWD(2026) 616. Why beta: the proposal is tabled, not final — it is entering trialogue (Parliament ↔ Council) — so figures and architecture are pending verification against the final Directive text. |

| `impact-assessment/`          | Impact Assessment — Modelling Results | **M · 38.** Reference library of the key modelling results in the ETS review impact assessment **SWD(2026) 616** (17 Jul 2026, 5 parts, 958 pp): 80 findings across carbon prices, mitigation costs, scenario/cap outcomes, technology scaling, industry projections, removals and macro impacts — each with verbatim key numbers, a supporting quote, the exact part + PDF page, and (for the figure/table pages) a rendered screenshot of the source page under `public/data/impact-assessment/shots/` for one-click verification. Closes with a curated slice of the supplementary techno-economic assumptions (E3-Modelling/PRIMES investment-cost trajectories). Findings are AI-extracted (one Sonnet agent per PDF part), flagged pending human verification. |
| `necpr-targets/`             | NECPR 2025 — What Actually Compares | **M · 40.** What the 2025 National Energy and Climate Progress Reports (Implementing Reg. (EU) 2022/2299, EEA Reportnet 3 dataflow) let you compare across Member States, and what they don't. Centrepiece is a hand-built mapping of the 209 "other national objectives" reported by 22 Member States across Annex I Table 4 (GHG), Annex II Table 7 (RES) and Annex IV Table 6 (EE) into **26 target families** — the workbooks supply no cross-country key, and names are useless as one (the transport family alone arrives under seven different names, in two languages, on three bases), so the mapping keys on the sector + unit + description fields. Each family shows how many Member States sit behind it, whether it is comparable, and per member the name exactly as submitted plus what has to be corrected first. Around it: the EU-27 net LULUCF aggregate (our sum of the 27 national series — the with-additional-measures sink peaks at −233 Mt in 2030 against the −310 Mt target and falls back to −199 Mt by 2040), the five Member States whose stated LULUCF "commitment" is a copy of their own projection, headline-target coverage by scope and year, and ten data-quality flags including the Annex I Table 2 defect where the allocation series is in Mt but labelled kt, so the table's own gap column subtracts kt from Mt. Extraction is reproducible via `scripts/extract-necpr-2025.py` → `public/data/necpr-2025/`; the written review is `docs-internal/necpr-2025-annex-i-ii-iv-review-2026-07-29.md`. Why beta: the family mapping is a manual reading pending Secretariat review, and the three order-of-magnitude entry errors are flagged, not fixed. |
| `policy-hierarchy/`          | EU Policy Hierarchy | **M · 42.** The EU legal order as **one collapsible figure**, across the whole acquis — not only climate: primary law (TEU, TFEU, Euratom, Charter) at the top, then per-domain branches (climate & energy, environment, trade, industry & competitiveness, health, security & defence, digital, transport, agriculture & fisheries, economy/finance/social) of secondary law (regulations, directives, decisions), tertiary law (delegated / implementing acts), soft law (communications, recommendations, green/white papers) and the supporting layer (impact assessments, SWDs), with the law-making machinery (Art. 288/290/291 TFEU, comitology, Better Regulation) called out under the treaty tier. Four cross-cutting **lenses — climate · competitiveness · security · health —** re-colour the whole tree so the same hierarchy reads as "the climate acquis", "the competitiveness acquis", etc.; every instrument carries its official citation and an **EUR-Lex link**, and a legend explains what each instrument type is and what it binds. Deliberately more structure and less movement than the Policy Navigator's force graph. On top of the lenses sit two Secretariat-facing layers: **mandate rings** — a working operationalisation of Art. 3(2)(b) of the Climate Law (the Board advises on the coherence of *Union measures*, not "climate measures"), sorting every instrument into **core** (the climate architecture), **adjacent** (other-purpose acts with material climate provisions, coherence-assessable) or **context** (shapes feasibility only), each with a one-line "Board angle", argued in a collapsible mandate panel and flagged as a working interpretation, not a legal opinion or Board position; and a **"moving now" status layer** marking where the 2025-26 agenda (omnibus simplification, ETS Phase-5 review, MFF 2028-34, Digital Omnibus) is reopening or revising instruments. Board-view preset, shareable URL state and full CSV export included. Why beta: the instrument register, ring assignments and status notes were compiled by Sonnet agents from model knowledge (uncertain entries flagged `unverified` / `?`, linking to a EUR-Lex search rather than a CELEX record) — a structural map pending Secretariat verification, not a citable register. |

## Promoting a beta module to production

The operation is deliberately mechanical — nothing in `beta/` is structurally
different from `src/app/`:

```bash
# promote the module back into the Next.js app tree
git mv beta/modules/energy-system src/app/energy-system

# re-add the link in the top navigation (SiteHeader.tsx and the home page)
# re-enable the relevant API routes under src/app/api if they were disabled
```

After that, `next build` picks the route back up automatically. No Next.js
config change, no manifest edit, no rewrite. That is the whole point of using
a filesystem-level parking lot instead of feature flags.

## What stays wired up

- **Shared components** (`src/components/ClimadaExplorer.tsx`, `PypsaLeafletMap.tsx`,
  `MediaMonitoringMap.tsx`, …) remain under `src/components/` because they are
  shared with other pages or are imported by beta pages when those pages are
  promoted.
- **API routes** that the beta pages relied on (`src/app/api/climada`,
  `src/app/api/media-monitoring/**`) remain under `src/app/api/` so that they
  can be exercised directly or reactivated without an extra move.
- **Data pipelines** under `scripts/` keep running for every module, beta or
  not; if EEA IT turns a pipeline off, it is a one-line change in the
  scheduler.

This keeps the beta graveyard cheap — promoting a module is a `git mv`, not a
refactor.
